# SolShield GitHub Actions Template
# Copy this to .github/workflows/solshield.yml in your repo

name: SolShield Security Audit

on:
  push:
    branches: [main, master]
    paths:
      - 'programs/**'
      - 'src/**'
      - '*.rs'
  pull_request:
    branches: [main, master]
    paths:
      - 'programs/**'
      - 'src/**'
      - '*.rs'

jobs:
  audit:
    name: Security Audit
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install SolShield
        run: |
          git clone https://github.com/oh-ashen-one/solshield.git /tmp/solshield
          cd /tmp/solshield/packages/cli && npm install && npm run build && npm link

      - name: Run Security Audit
        run: solshield ci . --fail-on high --sarif results.sarif
        continue-on-error: true

      - name: Upload SARIF to GitHub
        uses: github/codeql-action/upload-sarif@v3
        if: always()
        with:
          sarif_file: results.sarif

      - name: Check for Critical Issues
        run: |
          if grep -q '"level": "error"' results.sarif 2>/dev/null; then
            echo "❌ Critical security issues found!"
            exit 1
          fi
          echo "✅ No critical issues found"

  # Optional: Comment on PR with findings
  comment:
    name: PR Comment
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    needs: audit
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install SolShield
        run: |
          git clone https://github.com/oh-ashen-one/solshield.git /tmp/solshield
          cd /tmp/solshield/packages/cli && npm install && npm run build && npm link

      - name: Generate Markdown Report
        run: solshield audit . --format markdown > report.md

      - name: Comment on PR
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const report = fs.readFileSync('report.md', 'utf8');
            
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `## 🛡️ SolShield Security Report\n\n${report}`
            });
