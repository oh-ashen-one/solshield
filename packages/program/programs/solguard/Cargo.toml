[package]
name = "solguard"
version = "0.1.0"
description = "On-chain audit registry for Solana smart contracts"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "solguard"

[features]
default = []
cpi = ["no-entrypoint"]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
idl-build = ["anchor-lang/idl-build"]

[dependencies]
anchor-lang = "0.30.1"
