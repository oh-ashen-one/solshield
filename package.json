{
  "name": "solshield",
  "version": "0.1.0",
  "private": true,
  "description": "AI-Powered Smart Contract Auditor for Solana",
  "repository": {
    "type": "git",
    "url": "https://github.com/oh-ashen-one/solshield"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "cli": "pnpm --filter @solshield/cli",
    "web": "pnpm --filter @solshield/web",
    "dev": "pnpm --filter @solshield/cli dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cross-spawn": "^7.0.6",
    "ms": "^2.1.3"
  }
}
